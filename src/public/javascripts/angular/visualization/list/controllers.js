define(['angular', 'underscore'], function (ng, _) {
    'use strict';

    ng.module('visualizationList.controllers', []).
        controller('List',
        ['$scope', 'VisualizationService', '$q', '$location', '$routeParams', '$timeout',
            function ($scope, VisualizationService, $q, $location, $routeParams, $timeout) {

                
            }])
});