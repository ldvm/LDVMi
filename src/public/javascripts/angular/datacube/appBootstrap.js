/**
 * bootstraps angular onto the window.document node
 */
define([
    'require',
    'angular',
    'material',
    'app',
    'appConfig'
], function (require, ng, material) {
    'use strict';
});