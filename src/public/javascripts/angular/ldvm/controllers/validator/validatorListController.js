define(['angular', '../controllers'], function (ng) {
    'use strict';

    return ng.module('ldvm.controllers')
        .controller('ValidatorList', [
            '$scope',
            '$routeParams',
            function (
                $scope,
                $routeParams
            ){

            }]);
});